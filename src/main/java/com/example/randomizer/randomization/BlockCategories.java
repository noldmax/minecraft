package com.example.randomizer.randomization;

// No longer used. Block pool is now built dynamically from BuiltInRegistries.BLOCK
// in BlockDropRandomizer.initialize().
final class BlockCategories {
    private BlockCategories() {}
}
