package com.example.randomizer.mixin;

// Replaced by AbstractFurnaceBlockEntityMixin — AbstractCookingRecipe.assemble()
// is not directly injectable via @Inject on the class itself. Instead we use
// @Redirect on the call site inside AbstractFurnaceBlockEntity.burn().
public class AbstractCookingRecipeMixin {}
